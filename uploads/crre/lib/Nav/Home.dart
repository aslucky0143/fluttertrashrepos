import 'package:flutter/material.dart';

class NavHome extends StatelessWidget {
  const NavHome({Key? key}) : super(key: key);
  static const routename = 'NavHome';

  @override
  Widget build(BuildContext context) {
    return const Text('NavHome');
  }
}
